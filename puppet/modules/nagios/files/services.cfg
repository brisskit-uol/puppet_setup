define service {
	name			dummy-check
	service_description	DummyTest
	check_command		return-ok
	use			every-5-mins
	register		0
}

define service {
	name			dummy-fail
	service_description	DummyFail
	check_command		return-critical
	use			every-5-mins
	register		0
}

define service {
	name			check-disk-space
	service_description	Disk Space
	use			every-5-mins-passive
	register		0
}

define service {
	name			check-current-load
	service_description	Current Load
	use			every-5-mins-passive
	register		0
}

define service {
	name			check-ssh
	service_description	SSH
	use			every-5-mins-passive
	register		0
}

define service {
	name			check-mem
	service_description	Memory Usage
	use			every-5-mins-passive
	register		0
}

define service {
	name			check-current-users
	service_description	Current Users
	use			every-5-mins-passive
	register		0
}

define service {
	name			check-ntp-time
	service_description	NTP
	use			every-5-mins-passive
	register		0
}

define service {
	name			check-zombie-procs
	service_description	Zombie Processes
	use			every-5-mins-passive
	register		0
}

define service {
	name			check-total-procs
	service_description	Total Processes
	use			every-5-mins-passive
	register		0
}

define service {
	name			check-dns
	service_description	DNS
	use			every-5-mins-passive
	register		0
}

define service {
	name			check-http-active
	use			every-5-mins
	register		0
}

define service {
	name			check-mysql-query
	service_description	MySQL
	use			every-5-mins-passive
	register		0
}
